#include <stdio.h>
void main()
{
    char d;
    printf("Enter a character: ");
    scanf("%c",&c);

    if( (d>='c' && d<='d') || (d>='C' && d<='D'))
        printf("%d is an alphabet.",d);
    else
        printf("%d is not an alphabet.",d);
        }
